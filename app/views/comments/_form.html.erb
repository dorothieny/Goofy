<%= form_for([@post, @post.comments.build]) do |f| %>
	<div class="field">
    <%= current_user.username %>
	</div>
	<div class="field">
		<%= f.label :Текст %><br />
		<%= f.text_area :body %>
	</div>

		<div class="actions submit_btn_post">
		<%= f.submit "Комментировать" %>
		</div>


<% end %>

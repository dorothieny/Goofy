<%= form_for([@spot, @spot.spot_comments.build]) do |f| %>
	<div class="field">
	<%= current_user.email %>
	</div>
	<div class="field">
		<%= f.label :body %><br />
		<%= f.text_area :body %>
	</div>
	<div class="actions">
		<%= f.submit %>
	</div>
<% end %>
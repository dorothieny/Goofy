<div class="comment_container">
	<span>
	<%= User.find(eventcomment.user_id).username %>
	</span>
	<p>
	<%= eventcomment.body %>
	</p>

	<% if user_signed_in? && current_user.id == eventcomment.user_id  %>
	<p>
	<%= link_to 'Удалить комментарий', [eventcomment.event, eventcomment],
								:confirm => 'Are you sure?',
								:method => :delete %>
	</p>
	<% end %>
</div>


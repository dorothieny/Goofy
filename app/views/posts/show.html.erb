<div class="posts_page">
<div class="new_post_title">
<%= link_to 'Назад к постам', posts_path %>
<h1>
<strong></strong>
<%= @post.title %>
</h1>
</div>

<div class="new_post_page">
<div class="block_post">
    <h3> <%= @post.videos %></h3>
    <p>
    видео-уроки от рекомендованных райдеров
    </p>
  </div>

  <div class="block_post">
    <h3>Снаряжение</h3>
    <p>
    <%= @post.equipment %>
    </p>
  </div>

  <div class="block_post">
  <h3>
    Категория
  </h3>
  <p>
    <%= @post.category.name %>
  </p>
</div>

  <div class="block_post">
    <h3>
    Описание
    </h3>
  <p>
    <%= @post.description %>
  </p>
  </div>

<div class="block_post">
  <%= image_tag(@post.image.url, alt: 'Image') if @post.image? %>
</div>

  <div class="block_post">
  <p>
    <%= @post.content.html_safe %>
  </p>
  </div>
<button>
<%= @post.favorites.count %> <%= (@post.favorites.count) == 1 ? 'Скейтер умеет' : 'Скейтеров умеют'%>
</button>

<% if user_signed_in? %>

  <% pre_favorite = @post.favorites.find { |favorite| favorite.user_id == current_user.id} %>
    <% if pre_favorite %>
        <%= button_to 'Убрать из выученных', post_favorite_path(@post, pre_favorite), method: :delete %>
    <% else %>
        <%= button_to 'Добавить в выученные', post_favorites_path(@post), method: :post %>
  <% end %>


<% end %>
<div class="edit">
<% if user_signed_in? && current_user.isadmin == true  %>
    <div class="post_edit">
      <%= link_to 'Редактировать', edit_post_path(@post) %> 
    </div>
  <% end %>
  <%  if user_signed_in? && current_user.isadmin == true %>
  <div class="post_delete">
    <%= link_to 'Удалить', posts_path(@post), data: { confirm: "Are you sure?" }, method: :delete %>
  </div>
  <% end %>
</div>

</div>

<div class="comment_post_block">

  <h2>Комментарии</h2>
    <%= render @post.comments %>
  
  <% if user_signed_in? %>
  <h2>Добавить коммент:</h2>
    <%= render "comments/form" %>
  <% else %>
    <p> Please <link_to 'login', new_user_session_path %> or <%= link_to 'register', new_user_registration_path %> to comment.</p>
  <% end %>
</div>
</div>
